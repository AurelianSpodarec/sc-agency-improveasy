export interface User {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    createdOn: Date;
}

export interface UserPost {
    firstName: string;
    lastName: string;
    email: string;
}
